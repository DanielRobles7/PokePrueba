import React, { Component } from 'react';
/*export default class PokemonBucle extends Component {
   //crearTable
    state={  
        pokemon : null
    }
    createTable = () => {
        let table = []
    
        // Bucle externo para crear padre
        for (let i = 0; i < 5; i++) {
            let children = []
           // Bucle Interior para crear hijos
            for (let j = 0; j < 5; j++) {
                table.push(<tr>{children}</tr>)
            }
        }
        return table
    }
       render(){
               return (
           <table>
               {this.createTable()}
           </table>
        )
         }
}
*/

